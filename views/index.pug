extends layout

block content
  h1= title

  navbar 
    p
      a(href='/new') Add a new message
    //- p
    //-   a(href='/login') Login
    //- p
    //-   a(href='/logout') Logout

  p Welcome to #{title}

  ul
    each message in message_list
      li= message.message + " -- Written by, " + message.username + " (" + message.added + ")."
